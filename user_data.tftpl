#!/bin/bash
sudo apt update -y
sudo apt install -y nginx
sudo ufw allow 'Nginx HTTP'
cat <<EOF > /var/www/html/index.html
<html>
<h2>Welcome to ${server_owner} server</h2>
</html>
EOF
sudo systemctl start nginx